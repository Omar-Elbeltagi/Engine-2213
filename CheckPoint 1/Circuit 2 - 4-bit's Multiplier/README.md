The circuit shown is a 4-bit multiplier:

The circuit is based on the concept of binary multiplication, where multiplication is performed by generating partial products and then adding them together. In binary, each bit of the multiplier is used to multiply the multiplicand, similar to decimal multiplication but using only 0s and 1s.
The process begins with the least significant bit (LSB) of the multiplier (B1). This bit is ANDed with all bits of the multiplicand (A1–A4), producing the first partial product. If the bit is 1, the multiplicand is copied; if it is 0, the result is 0
<img width="950" height="766" alt="image" src="https://github.com/user-attachments/assets/a2db1104-8a66-40e8-80f5-2c80cfd3504c" />
